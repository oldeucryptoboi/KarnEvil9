{
  "name": "@openvger/api",
  "version": "0.1.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "dev": "tsc -b --watch",
    "clean": "rm -rf dist",
    "test": "vitest run"
  },
  "dependencies": {
    "@openvger/schemas": "workspace:*",
    "@openvger/kernel": "workspace:*",
    "@openvger/tools": "workspace:*",
    "@openvger/journal": "workspace:*",
    "@openvger/permissions": "workspace:*",
    "@openvger/plugins": "workspace:*",
    "express": "^5.0.0",
    "uuid": "^11.0.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.0",
    "@openvger/planner": "workspace:*",
    "@openvger/memory": "workspace:*"
  }
}
