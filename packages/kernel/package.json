{
  "name": "@openflaw/kernel",
  "version": "0.1.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -b",
    "dev": "tsc -b --watch",
    "clean": "rm -rf dist",
    "test": "vitest run"
  },
  "dependencies": {
    "@openflaw/schemas": "workspace:*",
    "@openflaw/journal": "workspace:*",
    "@openflaw/tools": "workspace:*",
    "@openflaw/permissions": "workspace:*",
    "@openflaw/memory": "workspace:*",
    "uuid": "^11.0.0"
  },
  "devDependencies": {
    "@openflaw/planner": "workspace:*"
  }
}
